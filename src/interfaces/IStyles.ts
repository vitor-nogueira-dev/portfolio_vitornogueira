export interface IStyles {
    innerWidth: string;
    interWidth: string;
    paddings: string;
    yPaddings: string;
    xPaddings: string;
    topPaddings: string;
    bottomPaddings: string;
    flexCenter: string;
    flexStart: string;
    flexEnd: string;
    navPadding: string;
    heroHeading: string;
    heroDText: string;
}